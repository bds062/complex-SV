# Benchmarks

All reported predictions are out-of-fold at the genome level.

- `localization/`: localized interval labels and the localization training protocol.
- `chromosome/`: chromosome-level labels and the chromosome training protocol.
- `ensemble/`: chromosome-level combinations of both methods.
